<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Elcode.Search" parent="@android:style/Widget.EditText">
        <item name="android:textColor">@color/black</item>
        <item name="android:paddingStart">20dp</item>
        <item name="android:height">30dp</item>
        <item name="android:background">@drawable/edit_text_corner</item>
        <item name="android:fontFamily">@font/baloo</item>
    </style>
    <style name="AppTheme" parent="Widget.AppCompat.SearchView">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <!--SearchView query text color-->
        <item name="android:textColorPrimary">@color/white</item>
        <!--SearchView query hint text color-->
        <item name="android:textColorHint">@color/white</item>
    </style>
</resources>